name: publish
run-name: Publish ${{ github.ref_name }}

on:
  release:
    types: [published]

jobs:
  mapache-py:
    if: startsWith(github.event.release.tag_name, 'mapache-py/')
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: mapache-py
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Install build tools
        run: pip install build twine

      - name: Build
        run: python -m build

      - name: Publish to PyPI
        run: twine upload dist/*
        env:
          TWINE_USERNAME: __token__
          TWINE_PASSWORD: ${{ secrets.PYPI_API_TOKEN }}

  mapache-go:
    if: startsWith(github.event.release.tag_name, 'mapache-go/')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: "1.26"

      - name: Warm Go module proxy
        run: |
          VERSION="${GITHUB_REF_NAME#mapache-go/}"
          GOPROXY=proxy.golang.org go list -m "github.com/gaucho-racing/mapache/mapache-go/v3@${VERSION}"
