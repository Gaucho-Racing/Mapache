component_management:
  default_rules:
    statuses:
      - type: patch
        target: auto
  individual_components:
    - component_id: dashboard
      name: Dashboard
      paths:
        - "dashboard/**"
    - component_id: ingest
      paths:
        - "ingest/**"
    - component_id: gr24
      paths:
        - "gr24/**"
    - component_id: rigby
      paths:
        - "rigby/**"